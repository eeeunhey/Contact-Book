1. useState를 언제 써야 할까?
컴포넌트 하나에서만( 또는 자식 컴포넌트에 한단계 정도만 내려가는)  필요한 데이터라면 useState를 쓰는 게 좋다!

버튼을 눌러서 UI가 바뀌는 경우
입력 필드(텍스트 입력, 체크박스 등)의 값을 저장하는 경우
토글(켜고 끄는 버튼) 같은 간단한 상태 변화
컴포넌트 내부에서만 쓰는 값 (다른 곳에서 필요하지 않음)
자식컴포넌트 한 단계정도만 내려가는 경우 


2. zustand 같은 상태 관리 라이브러리를 언제 써야 할까?
여러 컴포넌트에서 같은 데이터를 써야 한다면?
다른 페이지(컴포넌트)에서도 공유해야 한다면?
규모가 커질수록 관리하기 쉽게 만들고 싶다면?


3. npm install @mui/icons-material
npm install @mui/material @emotion/react @emotion/styled

4. 한쪽은 연락처를 입력하고 다른 한쪽은 연락처가 보이면서 검색할 수 있는 기능을 넣자

5. component/ ContactForm.jsx   || ContactList.jsx

6. 반반 보여주기 위해 MUI에 GRID를 사용하자 
콘테이너 안에 아이템을 추가한다
```jsx
<Grid container spacing={2}>
    <Grid size={6}> -사이즈를 지정한다
        <ContactForm/>
    </Grid>
        <Grid size={6}>
        <ContactList/>
    </Grid>
</Grid>

```

7. 반으로 나눴으니 이제 ContactFrom에 가서 만들어보장
입력을 위한 inputForm 을 만들자 Name / phoneNum -> TextFiled 로 인풋폼을 만들 수 있다
    <TextField id="기능 정의 이름 (name/phone 등등 넣으면 된다 )" label="Outlined" variant="Outlined>
8. mui 박스라는 컴포넌트를 제공하는데 div 같은 개념이다 
9. <Box display="flax" flexDirection="column" alignItems="center" gap={2}> 박스를 통해서 스타일을 줄 수 있다
10. <Button>넣기 
11. 텍스트 필드에 값 value={name} value={phoneNumber}
12. const [name, setName] = useState('')  / const[phoneNumber, setPhoneNumber]= useState('')
13. 컴포넌트별로 상태가 공유가 되는걸 주스탠드로 쓰자!
14.  <TextField id="기능 정의 이름 (name/phone 등등 넣으면 된다 )" label="Outlined" variant="Outlined" value={name} onChange={(e)=>setName(e.target.value)}>
15. 버튼 클릭 이벤트 지정하기 onClick={handleAddContact}
16. 버튼이 클리되면 추가되는 이벤트를 만들자 -> 데이터를 담아놀 공간이 필요하다!
  const handleAddContact = () => {
    // 연락처 저장 장소 -> 배열을 만들자 phoneBook = [] 
    // 배열을 어디에서 저장할가 -> 나중에 리스트에서 보여줄거다->
     두개의 컴포넌트가 데이터를 공유하고 있으므로 상태관리가 필요하다
    

  };

17. stores/usePhonebookStore.js
import {create} from "zustand"

const usePhoneBookStore = create(()=> ({
    phoneBook:[]
}))

export default usePhoneBookStore;

18. 이름이나 값이 잘 있나 확인하기 
if(name.trim() || phoneNumber.trim) return
handleAddContact 내부에 addContact() 함수 만들기

19.     addContact(name,phoneNumber,email,githubId)  상태관리 안에 만들자